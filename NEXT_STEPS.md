# Следующие шаги: Семейный ИИ-планировщик

**Дата:** 2026-01-07  
**Статус:** MVP-0.6 (50% готовности)

---

## ✅ Обновления требований

**Изменение:** Итерация 5 (поиск альтернативных слотов) удалена из требований.

**Причина:** Пришли к выводу, что календарь общий для обоих партнеров, поэтому поиск альтернативных слотов не требуется. При конфликте бот просто подсвечивает проблему и спрашивает пользователя что делать.

**Обновленные файлы:**
- ✅ `task.md` - убраны упоминания `find_common_slot` и альтернативных слотов
- ✅ `README.md` - обновлен список итераций
- ✅ `GAP_ANALYSIS.md` - обновлен анализ пробелов

---

## 🎯 Следующий шаг: Итерация 4 - Уведомления партнера

### Цель
Реализовать автоматические уведомления партнера о новых планах при создании события.

### Задачи

1. **Реализовать функцию `notify_partner_about_event` в `telegram_bot.py`**
   - Находит партнера создателя по `users.partner_telegram_id`
   - Формирует сообщение в спокойном тоне:
     - "Жена заняла субботу 10:00: секция у сына"
     - "Муж занял субботу 11:00: поехать за продуктами"
   - Отправляет сообщение партнеру через Telegram API (не через агента)
   - Устанавливает флаг `partner_notified=True` в БД через `mark_partner_notified`

2. **Интегрировать с `schedule_event`**
   - В `core_logic/calendar_tools.py` добавить вызов `notify_partner_about_event` при успешном создании события
   - Передавать параметр `notify_partner: bool = True` (по умолчанию True)
   - Обработать случай, когда партнер не найден (не падать с ошибкой)

3. **Обновить обертки инструментов**
   - В `agents_wrappers.py` добавить параметр `notify_partner` в `schedule_event` (опционально, по умолчанию True)

### Критерий готовности
При создании события одним партнером второму автоматически приходит уведомление в Telegram.

### Оценка времени
2-3 часа

---

## 📋 Рекомендуемый агент BMAD

Для реализации итерации 4 рекомендуется использовать:

**@bmad/bmm/agents/dev** - агент разработчика

**Почему:**
- Специализируется на реализации функциональности
- Работает с существующим кодом и архитектурой
- Понимает требования из технического задания
- Может интегрировать новые функции с существующим кодом

**Активация:**
```
@bmad/bmm/agents/dev
```

**Контекст для агента:**
- Текущая структура проекта (telegram_bot.py, core_logic/)
- Существующие функции работы с БД (database.py)
- Требования из task.md, раздел 3.1, пункт 4
- Gap-анализ из GAP_ANALYSIS.md

---

## 🔄 После завершения Итерации 4

**Следующий шаг:** Итерация 5 - Утренние дайджесты

**Приоритет:** 🔴 КРИТИЧНО для MVP

**Оценка времени:** 3-4 часа

---

## 📝 Примечания

1. **Архитектура уведомлений:**
   - Уведомления отправляются напрямую через Telegram API, не через агента
   - Это предотвращает рекурсию и бесконечные циклы
   - Используется функция `mark_partner_notified` из `database.py` для установки флага

2. **Обработка ошибок:**
   - Если партнер не найден - логировать предупреждение, но не падать
   - Если отправка уведомления не удалась - логировать ошибку, но не блокировать создание события

3. **Тестирование:**
   - Создать событие от первого пользователя → проверить уведомление второму
   - Создать событие от пользователя без партнера → проверить что не падает
   - Проверить установку флага `partner_notified` в БД

