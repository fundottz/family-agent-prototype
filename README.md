# Семейный ИИ-планировщик на Agno

ИИ-ассистент для координации семейных дел через Telegram.

## Быстрый старт

### 1. Настройка окружения

```bash
Убедись, что .env файл заполнен:
# - DEEPSEEK_API_KEY
# - TELEGRAM_BOT_TOKEN
# - DB_FILE=data/family_calendar.db (опционально, по умолчанию data/family_calendar.db)
# - TIMEZONE=Europe/Moscow (опционально)
```

### 2. Установка зависимостей

```bash
# Создай виртуальное окружение (если еще не создано)
python3 -m venv venv
source venv/bin/activate  # или: venv\Scripts\activate на Windows

# Установи зависимости
pip install -r requirements.txt
```

### 3. Запуск приложения

#### Запуск Telegram бота

```bash
# Простой способ (скрипт автоматически активирует venv)
./run.sh

# Или вручную:
source test-env/bin/activate  # или: venv/bin/activate
python main.py
```

Бот запустится и будет ожидать сообщения в Telegram.

#### Запуск AgentOS

AgentOS предоставляет REST API для взаимодействия с агентом:

```bash
# Простой способ (скрипт автоматически активирует venv)
./run_agentos.sh

# Или вручную:
source test-env/bin/activate
python agentos_app.py
```

После запуска AgentOS будет доступен по адресам:
- **App Interface**: `http://localhost:7777` - Используйте этот URL для подключения к AgentOS control plane
- **API Documentation**: `http://localhost:7777/docs` - Интерактивная документация API и тестирование
- **Configuration**: `http://localhost:7777/config` - Просмотр конфигурации AgentOS

Для подключения к AgentOS control plane используйте URL `http://localhost:7777`.

## Структура проекта

```
family-agent-prototype/
├── main.py                # Точка входа для Telegram бота, конфигурация агента
├── agentos_app.py         # AgentOS приложение (REST API)
├── telegram_bot.py        # Telegram бот
├── core_logic/            # Чистая бизнес-логика (framework-agnostic)
│   ├── __init__.py
│   ├── schemas.py         # Pydantic модели
│   ├── database.py        # Работа с SQLite БД
│   └── calendar_tools.py  # Функции работы с календарем
├── agents_wrappers.py     # Обертки тулов для Agno
├── scheduler.py           # Утренние дайджесты (будет создано)
├── data/                  # Директория для данных
│   └── family_calendar.db # SQLite база данных (создается автоматически)
├── config/                # Конфигурационные файлы
│   └── users.json         # Дефолтные пользователи (загружаются при первом запуске)
├── run.sh                 # Скрипт запуска Telegram бота
├── run_agentos.sh         # Скрипт запуска AgentOS
└── requirements.txt       # Зависимости
```

## Текущий статус

**Общий прогресс:** MVP-0.8 (80% готовности)

### ✅ Итерация 1: Базовая инфраструктура (MVP-0)
- [x] Структура проекта
- [x] Настройка окружения
- [x] Pydantic модели
- [x] SQLite база данных
- [x] Базовый Telegram бот
- [x] Минимальный Agno агент

**Критерий готовности:** Можно написать боту "Привет" и получить ответ ✅

### ✅ Итерация 2: Схема данных и модели
- [x] Все Pydantic модели описаны
- [x] Схема БД реализована (таблицы users, events, event_participants)
- [x] Функции работы с БД готовы

### ✅ Итерация 3: Работа с календарем (чтение и создание)
- [x] Функция `check_availability` - проверка конфликтов
- [x] Функция `schedule_event` - создание событий
- [x] Функция `get_today_agenda` - получение событий на день
- [x] Обертки инструментов для Agno
- [x] Инструменты добавлены в агента

**Критерий готовности:** 
- ✅ Можно написать "в субботу в 10 секция у сына" → событие создается
- ✅ Можно спросить "что у нас сегодня?" → получаем список событий
- ✅ События видны обоим партнерам (общий календарь)

### ✅ Итерация 4: Уведомления партнера
- [x] Функция `notify_partner_about_event` реализована
- [x] Интеграция с `schedule_event` через callback
- [x] Отправка уведомлений через Telegram API
- [x] Установка флага `partner_notified` в БД

**Критерий готовности:** При создании события партнер автоматически получает уведомление ✅

### 📋 Следующие итерации
- ❌ Итерация 5: Утренние дайджесты (КРИТИЧНО для MVP)
- ⚠️ Итерация 6: Полировка и тестирование

**Примечание:** Поиск альтернативных слотов не требуется, так как календарь общий для обоих партнеров.

## Получение ключей

### База данных
Проект использует SQLite для хранения данных. База данных создается автоматически при первом запуске в файле `data/family_calendar.db`. Директория `data/` создается автоматически, если её нет. Можно указать другой путь через переменную окружения `DB_FILE`.

### Дефолтные пользователи
При первом запуске приложения автоматически проверяется наличие пользователей в таблице `users`. Если таблица пуста, пользователи загружаются из файла `config/users.json`. Формат файла:
```json
{
  "users": [
    {
      "telegram_id": 123456789,
      "name": "Имя пользователя",
      "partner_telegram_id": 987654321,
      "digest_time": "07:00"
    }
  ]
}
```
Поле `digest_time` опционально, по умолчанию используется "07:00".

### DeepSeek
1. Зарегистрируйся на https://platform.deepseek.com
2. Создай API ключ
3. Скопируй в `DEEPSEEK_API_KEY`

### Telegram Bot
1. Напиши @BotFather в Telegram
2. `/newbot` → следуй инструкциям
3. Скопируй токен в `TELEGRAM_BOT_TOKEN`

## Тестирование

После запуска бота:
1. Найди своего бота в Telegram
2. Отправь `/start`
3. Напиши "Привет" - бот должен ответить дружелюбно
4. Напиши "в субботу в 10 секция у сына" - событие должно создаться
5. Напиши "что у нас сегодня?" - должен показать список событий

## Правила коммитов

**Commit messages должны быть короткими и в одно предложение.**

Примеры:
- ✅ `Добавлен парсинг естественного языка для событий`
- ✅ `Исправлена ошибка с event loop в telegram_bot`
- ❌ `Итерация 3: Парсинг естественного языка\n\n- Добавлена функция parse_natural_language_event\n- Интегрирован в агента как tool`

Git настроен использовать шаблон `.gitmessage` для коммитов.
