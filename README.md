# Семейный ИИ-планировщик на Agno

ИИ-ассистент для координации семейных дел через Telegram.

## Быстрый старт

### 1. Настройка окружения

```bash
6нщдбзще# Убедись, что .env файл заполнен:
# - SUPABASE_URL, SUPABASE_KEY, SUPABASE_SERVICE_ROLE_KEY
# - DEEPSEEK_API_KEY
# - TELEGRAM_BOT_TOKEN
# - TIMEZONE=Europe/Moscow
```

### 2. Установка зависимостей

```bash
# Создай виртуальное окружение (если еще не создано)
python3 -m venv venv
source venv/bin/activate  # или: venv\Scripts\activate на Windows

# Установи зависимости
pip install -r requirements.txt
```

### 3. Запуск бота

```bash
# Простой способ (скрипт автоматически активирует venv)
./run.sh

# Или вручную:
source test-env/bin/activate  # или: venv/bin/activate
python main.py
```

Бот запустится и будет ожидать сообщения в Telegram.

## Структура проекта

```
family-agent-prototype/
├── main.py                # Точка входа, конфигурация агента
├── telegram_bot.py        # Telegram бот
├── core_logic/            # Чистая бизнес-логика (framework-agnostic)
│   ├── schemas.py         # Pydantic модели
│   └── supabase_client.py # Supabase клиент
├── agents_wrappers.py     # Обертки тулов для Agno (будет создано)
├── scheduler.py           # Утренние дайджесты (будет создано)
└── requirements.txt       # Зависимости
```

## Текущий статус

### ✅ Итерация 1: Базовая инфраструктура (MVP-0)
- [x] Структура проекта
- [x] Настройка окружения
- [x] Pydantic модели
- [x] Схема БД в Supabase
- [x] Базовый Telegram бот
- [x] Минимальный Agno агент

**Критерий готовности:** Можно написать боту "Привет" и получить ответ ✅

### 📋 Следующие итерации
- Итерация 2: Схема данных и модели (уже готово)
- Итерация 3: Парсинг естественного языка
- Итерация 4: Работа с календарем (чтение)
- Итерация 5: Создание событий
- Итерация 6: Уведомления партнера
- Итерация 7: Поиск альтернативных слотов
- Итерация 8: Утренние дайджесты
- Итерация 9: Уточнение продолжительности
- Итерация 10: Полировка и тестирование

## Получение ключей

### Supabase
- URL и ключи уже настроены через MCP
- Service Role Key нужно получить из Dashboard → Settings → API

### DeepSeek
1. Зарегистрируйся на https://platform.deepseek.com
2. Создай API ключ
3. Скопируй в `DEEPSEEK_API_KEY`

### Telegram Bot
1. Напиши @BotFather в Telegram
2. `/newbot` → следуй инструкциям
3. Скопируй токен в `TELEGRAM_BOT_TOKEN`

## Тестирование

После запуска бота:
1. Найди своего бота в Telegram
2. Отправь `/start`
3. Напиши "Привет"
4. Бот должен ответить дружелюбно
