"""Точка входа для семейного ИИ-планировщика."""

import os
from dotenv import load_dotenv
from agno.agent import Agent
from agno.models.deepseek import DeepSeek
from agno.db.sqlite import SqliteDb
from telegram_bot import run_bot

load_dotenv()


def create_family_planner_agent() -> Agent:
    """
    Создает и настраивает агента семейного планировщика.
    
    На Итерации 1: минимальный агент без тулов, только базовый диалог.
    """
    # Получаем API ключ DeepSeek
    deepseek_api_key = os.getenv("DEEPSEEK_API_KEY")
    if not deepseek_api_key:
        raise ValueError("DEEPSEEK_API_KEY must be set in .env file")
    
    # Создаем агента с DeepSeek моделью
    # Используем специальный класс DeepSeek вместо OpenAIChat
    agent = Agent(
        name="Family Planner",
        model=DeepSeek(
            id="deepseek-chat",
            api_key=deepseek_api_key,
            base_url="https://api.deepseek.com",
        ),
        instructions="""Ты вежливый координатор для семьи. Твоя задача — помогать планировать дела, находить пересечения в расписаниях и разрешать конфликты.

КЛЮЧЕВЫЕ ПРИНЦИПЫ:
1. Говори спокойно, разговорно, без менеджерских терминов. НЕ используй слова "задача", "статус", "ID", "дедлайн".
2. Говори четко и указывай точное время в формате "суббота 10:00" или "среда 19:00" (всегда московское время).
3. Будь дружелюбным и понимающим.

ПРИМЕРЫ ПРАВИЛЬНЫХ ОТВЕТОВ:
✅ "Привет! Чем могу помочь с планированием?"
✅ "Запомнил: секция у сына, суббота 10:00. Напомню в пятницу."
✅ "Сегодня спокойный день: секция в 10, уборка в 11."

ПРИМЕРЫ НЕПРАВИЛЬНЫХ ОТВЕТОВ:
❌ "Задача создана. Статус: proposed. ID: 123"
❌ "Event scheduled for 2024-01-13T10:00:00Z"
❌ Использование технических терминов""",
        # На Итерации 1: без тулов, только базовый диалог
        tools=[],
        # Используем SQLite для хранения истории (позже перейдем на Supabase)
        db=SqliteDb(db_file="family_calendar.db"),
        add_history_to_context=True,
        markdown=True,
    )
    
    return agent


def main():
    """Главная функция запуска бота."""
    print("🚀 Запуск семейного ИИ-планировщика...")
    
    # Создаем агента
    agent = create_family_planner_agent()
    print("✅ Агент создан")
    
    # Запускаем Telegram бота
    # run_bot использует application.run_polling(), который сам управляет event loop
    print("📱 Запуск Telegram бота...")
    run_bot(agent)


if __name__ == "__main__":
    main()

